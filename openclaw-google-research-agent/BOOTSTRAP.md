On first run:

1. Confirm that your workspace tools are available.
2. Read `AGENTS.md`, `SOUL.md`, `IDENTITY.md`, and `USER.md`.
3. Inspect `tools/google_research_digest.py` so you know the local helper path.
4. When asked to analyze repositories, prefer primary repository evidence and
   use the helper script for batch collection or normalization.
